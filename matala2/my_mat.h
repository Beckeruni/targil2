// gets info from user, and creates a matrix accordingly 
int * getMatrix();

// gets start and finish, returns true if there is a path between both
bool isPath();

// gets start and finish, return value of shortest path
int shortestPath();